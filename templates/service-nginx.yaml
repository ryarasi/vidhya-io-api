apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: {{ .Values.service.nginx.type }}
  ports:
    - port: {{ .Values.service.nginx.port }}
      targetPort: 80
      nodePort: 30080
  selector:
    app: nginx
