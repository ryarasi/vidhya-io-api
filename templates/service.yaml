apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  type: NodePort
  ports:
    - port: {{ .Values.service.django.port }}
      targetPort: {{ .Values.service.django.port }}
      nodePort: 30000
  selector:
    app: web
